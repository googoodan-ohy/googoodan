(function(root){
const api=root.GradeMath,base=api.generate;
// Each row maps an authored worksheet to the standards it practices.
const additions={
1:[
['missing','Missing numbers','1.OA.A.1;1.OA.B.4;1.OA.D.8','unknown'],['three','Three numbers together','1.OA.A.2','three'],['facts','Related facts','1.OA.B.3–4','facts'],['count-on','Count on and back','1.OA.C.5','numberline'],['equal','True or false equations','1.OA.D.7','equal'],['count120','Number paths to 120','1.NBT.A.1','sequence'],['compare','Compare two-digit numbers','1.NBT.B.3','compare'],['add100','Add using tens and ones','1.NBT.C.4','arithmetic'],['ten-more','Ten more, ten less','1.NBT.C.5','mental'],['tens-sub','Subtract multiples of ten','1.NBT.C.6','arithmetic'],['order-length','Order and compare lengths','1.MD.A.1','lengthOrder'],['unit-length','Measure with equal units','1.MD.A.2','ruler'],['shape-build','Build and draw shapes','1.G.A.2','compose'],['halves','Halves and fourths','1.G.A.3','partition']],
2:[
['stories','Two-step stories','2.OA.A.1','twoStep'],['facts20','Fluent facts within 20','2.OA.B.2','arithmetic'],['odd-even','Odd and even groups','2.OA.C.3','parity'],['arrays','Rows and columns','2.OA.C.4','array'],['skip','Skip-count to 1000','2.NBT.A.2','sequence'],['number-names','Number names and expanded form','2.NBT.A.3','names'],['compare','Compare three-digit numbers','2.NBT.A.4','compare'],['four-add','Add four numbers','2.NBT.B.6','three'],['calc1000','Add and subtract within 1000','2.NBT.B.7,9','arithmetic'],['mental','Ten or a hundred more','2.NBT.B.8','mental'],['measure-tools','Choose a measuring tool','2.MD.A.1','measureTools'],['measure-units','Measure in two units','2.MD.A.2','ruler'],['estimate-length','Estimate and measure','2.MD.A.3','estimate'],['length-story','Length stories','2.MD.B.5','twoStep'],['numberline','Numbers on a line','2.MD.B.6','numberline'],['lineplot','Measurement line plots','2.MD.D.9','lineplot'],['graphs','Read and build graphs','2.MD.D.10','graph'],['shape','Shape attributes','2.G.A.1','shape'],['tile','Tile a rectangle','2.G.A.2','array'],['partition','Halves, thirds and fourths','2.G.A.3','partition']],
3:[
['groups','Equal groups and sharing','3.OA.A.1–3','array'],['unknown','Missing factors and quotients','3.OA.A.4;3.OA.B.6','unknown'],['properties','Multiplication strategies','3.OA.B.5','facts'],['stories','Two-step operation stories','3.OA.D.8','twoStep'],['patterns','Arithmetic patterns','3.OA.D.9','sequence'],['round','Round to tens and hundreds','3.NBT.A.1','round'],['calc1000','Add and subtract within 1000','3.NBT.A.2','arithmetic'],['tens-multiply','Multiply by multiples of ten','3.NBT.A.3','arithmetic'],['fraction-line','Fractions on a number line','3.NF.A.2','fractionLine'],['fraction-compare','Equivalent and ordered fractions','3.NF.A.3','fractionCompare'],['elapsed','Time to the minute','3.MD.A.1','elapsed'],['mass','Mass and liquid volume','3.MD.A.2','measureStory'],['lineplot','Halves and fourths on line plots','3.MD.B.4','lineplot'],['area-tiles','Square units and tiling','3.MD.C.5–6','array'],['area-parts','Area by adding parts','3.MD.C.7','areaParts'],['quadrilaterals','Sort quadrilaterals','3.G.A.1','shapeClass'],['equal-area','Equal-area parts','3.G.A.2','partition']],
4:[
['times-as-many','Times as many','4.OA.A.1–2','timesCompare'],['stories','Multi-step stories and remainders','4.OA.A.3','twoStep'],['factors','Factors, multiples and primes','4.OA.B.4','factors'],['patterns','Rules and number patterns','4.OA.C.5','sequence'],['ten-times','Ten times the place value','4.NBT.A.1','powers'],['compare','Compare large numbers','4.NBT.A.2','compare'],['round','Round to any whole-number place','4.NBT.A.3','round'],['large-calc','Multi-digit addition and subtraction','4.NBT.B.4','arithmetic'],['large-mul','Four-digit and two-digit products','4.NBT.B.5','arithmetic'],['long-div','Four-digit division','4.NBT.B.6','arithmetic'],['fraction-compare','Compare unlike fractions','4.NF.A.2','fractionCompare'],['fraction-sub','Subtract like and mixed fractions','4.NF.B.3','fractionArithmetic'],['fraction-whole','Multiply fractions by whole numbers','4.NF.B.4','fractionArithmetic'],['tenths-hundredths','Tenths and hundredths','4.NF.C.5–6','decimalFraction'],['decimal-compare','Compare decimals','4.NF.C.7','decimalCompare'],['convert','Measurement conversions','4.MD.A.1','convert'],['measure-stories','Measurement word problems','4.MD.A.2','measureStory'],['area-perimeter','Area and perimeter formulas','4.MD.A.3','areaParts'],['lineplot','Fraction measurement line plots','4.MD.B.4','lineplot'],['protractor','Measure and draw angles','4.MD.C.5–6','protractor'],['lines','Lines, rays and right angles','4.G.A.1','lines'],['shape-class','Classify triangles and quadrilaterals','4.G.A.2','shapeClass'],['symmetry','Lines of symmetry','4.G.A.3','symmetry']],
5:[
['expressions','Evaluate grouped expressions','5.OA.A.1','expressions'],['write-expressions','Write numerical expressions','5.OA.A.2','expressions'],['paired-patterns','Patterns and ordered pairs','5.OA.B.3','paired'],['place-ratio','Ten times and one tenth','5.NBT.A.1','powers'],['powers10','Powers of ten','5.NBT.A.2','powers'],['decimal-place','Decimal names and expanded form','5.NBT.A.3','decimalNames'],['decimal-compare','Compare decimals to thousandths','5.NBT.A.3','decimalCompare'],['round-decimal','Round decimals','5.NBT.A.4','round'],['whole-mul','Multi-digit whole-number products','5.NBT.B.5','arithmetic'],['whole-div','Two-digit divisors','5.NBT.B.6','arithmetic'],['decimal-mul','Multiply decimals','5.NBT.B.7','decimalArithmetic'],['decimal-div','Divide decimals exactly','5.NBT.B.7','decimalArithmetic'],['fraction-sub','Subtract unlike and mixed fractions','5.NF.A.1','fractionArithmetic'],['fraction-story','Fraction stories and estimation','5.NF.A.2','fractionStory'],['fraction-division','Fractions as division','5.NF.B.3','fractionDivision'],['fraction-area','Area with fractional sides','5.NF.B.4','fractionArithmetic'],['scaling','Multiplication as scaling','5.NF.B.5','scaling'],['fraction-products','Fraction and mixed-number stories','5.NF.B.6','fractionStory'],['unit-sharing','Unit-fraction sharing stories','5.NF.B.7','fractionDivision'],['convert','Convert measurement units','5.MD.A.1','convert'],['lineplot','Fraction data and line plots','5.MD.B.2','lineplot'],['volume-cubes','Volume in unit cubes','5.MD.C.3–4','cubes'],['volume-parts','Add volumes of joined prisms','5.MD.C.5','volumeParts'],['plot-points','Plot and interpret points','5.G.A.1–2','paired'],['shape-properties','Shape properties and families','5.G.B.3','shapeClass'],['shape-hierarchy','Shape classification hierarchy','5.G.B.4','shapeClass']]
};
for(const [g,rows] of Object.entries(additions))api.grades[g].units.push(...rows);
const box='<span class="write-box"></span>',space='<div class="drawing-space"></div>';
const svg=(s,w=210,h=80)=>'<div class="art">'+`<svg viewBox="0 0 ${w} ${h}" aria-hidden="true">${s}</svg></div>`;
const gcd=(a,b)=>b?gcd(b,a%b):Math.abs(a),fraction=(n,d)=>{const k=gcd(n,d);return d/k===1?String(n/k):n/k+'/'+(d/k)},F=(n,d)=>`<span class="fraction"><span>${n}</span><span>${d}</span></span>`;
const words=n=>{const small=['zero','one','two','three','four','five','six','seven','eight','nine','ten','eleven','twelve','thirteen','fourteen','fifteen','sixteen','seventeen','eighteen','nineteen'];if(n<20)return small[n];if(n<100)return ['','','twenty','thirty','forty','fifty','sixty','seventy','eighty','ninety'][Math.floor(n/10)]+(n%10?'-'+small[n%10]:'');return small[Math.floor(n/100)]+' hundred'+(n%100?' '+words(n%100):'')};
function extended(grade,id,seed){const entry=api.grades[grade].units.find(u=>u[0]===id);if(!entry)throw Error('Unknown worksheet '+id);if(!entry[3])return base(grade,id,seed);const kind=entry[3];let state=seed>>>0;const r=n=>{state=(Math.imul(state,1664525)+1013904223)>>>0;return Math.floor(state/4294967296*n)},pick=a=>a[r(a.length)];
const make=mode=>{let prompt='',visual='',task=box,answer='',open=false;const set=(p,v,t,a,o=false)=>{prompt=p;visual=v;task=t;answer=String(a);open=o};let a=2+r(8),b=2+r(8),c=1+r(8);
const grid=(rows,cols)=>svg(Array.from({length:rows*cols},(_,i)=>`<rect x="${10+i%cols*18}" y="${5+Math.floor(i/cols)*18}" width="18" height="18" fill="#e2edf1" stroke="#527284"/>`).join(''),Math.max(100,cols*18+20),rows*18+10);
if(kind==='unknown'){const mul=grade>=3,n=mul?a*b:a+b;set(mode===2?`A team ${mul?'packs '+a+' equal boxes':'has '+a+' tokens'}. ${mul?'There are '+n+' tokens altogether. How many in each box?':'After getting some more, it has '+n+'. How many were added?'}`:'Find the missing number.','',mode===0?`${a} ${mul?'×':'+'} ${box} = ${n}`:mode===1?`${box} ${mul?'÷':'−'} ${a} = ${b}`:box,mode===1?n:b);}
else if(kind==='three'){const count=grade===1?3:4,vals=Array.from({length:count},()=>grade===1?1+r(6):10+r(90)),sum=vals.reduce((x,y)=>x+y);set(mode===2?'These baskets contain the numbers of shells shown. How many shells altogether?':'Add the numbers. Show a helpful grouping.','',vals.join(' + ')+' = '+box+space,sum);}
else if(kind==='facts'){const mul=grade>=3,op=mul?'×':'+',n=mul?a*b:a+b;set(mode===0?'Use the related fact.':mode===1?'Complete the inverse fact.':'Explain how one fact helps you find the other.','',mode===1?`${n} ${mul?'÷':'−'} ${a} = ${box}`:`${a} ${op} ${b} = ${n}<br>${b} ${op} ${a} = ${box}`+(mode===2?space:''),mode===1?b:mode===2?`${n}. The order of the ${mul?'factors':'addends'} can change without changing the result.`:n,mode===2);}
else if(kind==='equal'){const n=a+b,offset=r(2);set('Is the equation true or false?','',`${a} + ${b} = ${n+offset}<br>True / False`,offset?'False':'True');}
else if(kind==='arithmetic'){let op=mode===1?'−':'+';if(id==='facts20'){a=r(11);b=r(11);if(a+b>20)b=20-a;}else if(id==='add100'){a=10+r(70);b=mode===0?r(10):10*(1+r(Math.floor((99-a)/10)));op='+';}else if(id==='tens-sub'){a=(2+r(8))*10;b=(1+r(a/10))*10;op='−';}else if(id.includes('mul')){a=id==='tens-multiply'?1+r(9):id==='large-mul'?(mode===0?1000+r(8000):10+r(90)):100+r(900);b=id==='tens-multiply'?10*(1+r(9)):id==='large-mul'?(mode===0?2+r(8):10+r(90)):10+r(90);op='×';}else if(id.includes('div')){b=id==='long-div'?2+r(8):10+r(90);const q=10+r(Math.floor(9000/b)-10),rem=grade===4?r(b):0;a=b*q+rem;op='÷';}else{const max=grade===2||grade===3?999:999999;a=10+r(max-10);b=10+r(max-10);if(op==='+'&&a+b>max)b=max-a;}if(op==='−'&&a<b)[a,b]=[b,a];const ans=op==='+'?a+b:op==='−'?a-b:op==='×'?a*b:Math.floor(a/b)+(a%b?' R '+a%b:'');set(mode===2?'Solve and explain a strategy you used.':'Calculate.','',`${a} ${op} ${b} = ${box}`+(mode===2?space:''),ans);}
else if(kind==='mental'){a=grade===1?10+r(80):100+r(801);b=grade===1?10:pick([10,100]);const plus=mode!==1;set(`Find ${b} ${plus?'more':'less'} than ${a}.`,'',box,plus?a+b:a-b);}
else if(kind==='sequence'){const step=grade===1?1:grade===2?pick([5,10,100]):2+r(8),max=grade===1?120:grade===2?1000:200,start=r(Math.max(1,max-step*4));set(`Start at ${start}. Add ${step} each time.`,'',`${start} → ${start+step} → ${box} → ${box}`,`${start+step*2}, ${start+step*3}`);}
else if(kind==='compare'||kind==='decimalCompare'){let max=grade===1?100:grade===2?1000:1000000;a=r(max);b=r(max);if(kind==='decimalCompare'){a=r(1000)/(grade===4?100:1000);b=r(1000)/(grade===4?100:1000);}set(mode===2?'Compare. Explain using place value.':'Write <, >, or =.','',`${a} ${box} ${b}`+(mode===2?space:''),a<b?'<':a>b?'>':'=');}
else if(kind==='names'){const n=100+r(900);set(mode===0?'Write this number using digits.':mode===1?'Write this number in words.':'Write the expanded form.','',`<p>${mode===0?words(n):n}</p>`+space,mode===0?n:mode===1?words(n):`${Math.floor(n/100)*100} + ${Math.floor(n/10)%10*10} + ${n%10}`);}
else if(kind==='numberline'){const start=grade===1?r(11):r(60),step=1+r(5);visual=svg('<path d="M15 35H195" stroke="#527284"/>'+Array.from({length:11},(_,i)=>`<path d="M${15+i*18} 30v10" stroke="#527284"/><text x="${15+i*18}" y="55" text-anchor="middle" font-size="9">${start+i}</text>`).join(''));set(`Start at ${start}. Make ${step} jumps to the right. Where do you land?`,visual,box,start+step);}
else if(kind==='twoStep'||kind==='timesCompare'){if(kind==='timesCompare')set(`Lee has ${a} cards. Sam has ${b} times as many. ${mode===1?'How many more cards does Sam have than Lee?':'How many cards does Sam have?'}`,'',box,mode===1?a*b-a:a*b);else if(grade<=2)set(id==='length-story'?`Join ribbons ${a+b} cm and ${c} cm long. Cut off ${a} cm. How many centimeters remain?`:`There are ${a+b} shells. Find ${c} more, then give away ${a}. How many remain?`,'',box,b+c);else if(mode===2&&grade===4){const total=a*b+c;set(`${total} students ride in vans holding ${b} students each. How many vans are needed?`,'',box,Math.ceil(total/b));}else {c=Math.min(c,a*b);set(`${a} boxes hold ${b} pencils each. ${c} pencils are used. How many are left?`,'',box,a*b-c);}}
else if(kind==='parity'){const n=1+r(20);set(mode===0?'Is this number odd or even?':mode===1?'Draw pairs. Is there one left over?':'For an even number, write two equal addends. For an odd number, write "odd".','',`<div class="equation">${n}</div>`+space,mode===0?(n%2?'Odd':'Even'):mode===1?(n%2?'Yes, one is left over.':'No, all objects are paired.'):(n%2?'odd':`${n/2} + ${n/2}`));}
else if(kind==='array'){a=1+r(5);b=1+r(5);set(mode===0?'How many tiles?':mode===1?'Write a repeated-addition equation for the rows.':'Write the total. Explain how the rows help.',grid(a,b),mode===1?space:box,mode===1?Array(a).fill(b).join(' + ')+' = '+a*b:a*b);}
else if(kind==='lengthOrder'){const vals=[a,a+3,a+7];if(r(2))vals.reverse();set(mode===0?'List A, B, C from shortest to longest.':'Which strip is longest?',svg(vals.map((v,i)=>`<text x="2" y="${18+i*23}">${'ABC'[i]}</text><rect x="22" y="${4+i*23}" width="${v*8}" height="16" fill="#9ec3c1"/>`).join('')),space,mode===0?vals.map((v,i)=>[v,'ABC'[i]]).sort((x,y)=>x[0]-y[0]).map(x=>x[1]).join(', '):'ABC'[vals.indexOf(Math.max(...vals))]);}
else if(kind==='ruler'){const n=2+r(7);set(grade===1?'How many equal units long is the strip?':mode===2?'The strip is measured using 1 cm units. How many millimeters long is it?':'Read the length in centimeters.',svg(`<rect x="15" y="5" width="${n*18}" height="16" fill="#a9c8d2"/><path d="M15 37H195" stroke="#527284"/>`+Array.from({length:11},(_,i)=>`<path d="M${15+i*18} 32v10" stroke="#527284"/><text x="${15+i*18}" y="55" text-anchor="middle" font-size="8">${i}</text>`).join('')),box,grade===2&&mode===2?n*10:n);}
else if(kind==='measureTools'){const items=[['a pencil','centimeter ruler'],['a classroom floor','meter stick or measuring tape'],['a book cover','centimeter ruler']][mode];set(`Choose a tool to measure ${items[0]}.`,'','ruler / measuring tape / clock',items[1]);}
else if(kind==='estimate'){set('Choose a book. Estimate its length in centimeters. Measure it with a ruler. Compare.','',`Estimate: ${box} cm<br>Measured: ${box} cm<br>Difference: ${box} cm`,'Answers vary. Check ruler starts at zero, unit is cm, and difference matches the two recorded lengths.',true);}
else if(kind==='compose'||kind==='partition'){if(kind==='compose')set(mode===0?'Draw a rectangle made from two triangles.':mode===1?'Draw a new shape made from two squares.':'Use blocks to build a larger solid from small cubes. Sketch your model.','',space,'Answers vary. Check that the requested pieces compose the drawn shape without gaps or overlaps.',true);else {const parts=grade===1?pick([2,4]):pick([2,3,4]);set(`Divide the rectangle into ${parts} equal-area parts. Shade one part.`,svg('<rect x="20" y="10" width="170" height="55" fill="white" stroke="#527284"/>'),'Each part is '+box+' of the whole.',`1/${parts}. Check ${parts} equal-area parts and one shaded part.`,true);}}
else if(kind==='round'){const decimal=grade===5,n=decimal?(100+r(9000))/100:10+r(grade===3?980:999980),place=decimal?pick([1,.1]):grade===3?pick([10,100]):pick([10,100,1000]);const ans=Math.round((n+Number.EPSILON)/place)*place;set(`Round ${n} to the nearest ${decimal?(place===1?'whole number':'tenth'):place}.`,'',box,Number(ans.toFixed(2)));}
else if(kind==='powers'){a=grade===4?(1+r(9))*100:Math.round((1+r(999))/10*100)/100;const power=grade===4?1:1+r(3),factor=10**power;set(mode===1?`Divide ${a} by ${factor}.`:`Multiply ${a} by ${factor}.`,'',box,Number((mode===1?a/factor:a*factor).toFixed(6)));}
else if(kind==='decimalNames'){const n=1+r(999);set(mode===0?'Write this decimal.':mode===1?'Write as thousandths.':'Write in expanded form.','',mode===0?`${n} thousandths = ${box}`:`${(n/1000).toFixed(3)}`+space,mode===0?(n/1000).toFixed(3):mode===1?`${n}/1000`:`${Math.floor(n/100)} × 1/10 + ${Math.floor(n/10)%10} × 1/100 + ${n%10} × 1/1000`);}
else if(kind==='decimalArithmetic'){const div=id==='decimal-div';a=1+r(99);b=1+r(9);if(div){const den=mode===1?b/10:b,q=(1+r(mode===1?99:999))/(mode===1?10:100),num=Math.round(q*den*100)/100;set(mode===2?'Find the exact quotient. Do not round.':'Divide exactly.','',`${num} ÷ ${den} = ${box}`,q);}else set('Multiply.','',`${a/10} × ${b/10} = ${box}`,a*b/100);}
else if(kind==='fractionLine'){const d=pick([2,3,4,6,8]),n=1+r(d-1);set('What fraction is marked?',svg('<path d="M15 35H195" stroke="#527284"/>'+Array.from({length:d+1},(_,i)=>`<path d="M${15+i*180/d} 30v10" stroke="#527284"/>`).join('')+`<circle cx="${15+n*180/d}" cy="35" r="4" fill="#534599"/><text x="15" y="58">0</text><text x="190" y="58">1</text>`),box,fraction(n,d));}
else if(kind==='fractionCompare'){const d=pick([2,3,4,6,8]),e=grade===3?d:pick([2,3,4,6,8]),n=1+r(d),m=1+r(e);set('Compare the fractions of the same-sized whole.','',`${F(n,d)} ${box} ${F(m,e)}`,n*e<m*d?'<':n*e>m*d?'>':'=');}
else if(kind==='fractionArithmetic'||kind==='fractionStory'||kind==='fractionDivision'||kind==='scaling'){let d=2+r(7),e=grade===4?d:2+r(7),n=1+r(d-1),m=1+r(e-1);if(kind==='scaling'){const numerator=mode===0?d-1:mode===1?d:d+1;set(`Without multiplying, compare ${a} × ${numerator}/${d} with ${a}.`,'','less / equal / greater',numerator<d?'less':numerator===d?'equal':'greater');}else if(kind==='fractionDivision'){if(id==='unit-sharing'){set(`Share 1/${d} liter equally among ${a} cups. How much goes in each cup?`,'',box+' liters',fraction(1,d*a));}else set(`Share ${a} sandwiches equally among ${b} people. How much per person?`,'',box,fraction(a,b));}else {const multiply=id==='fraction-whole'||id==='fraction-area'||id==='fraction-products';if(id==='fraction-whole'){m=a;e=1;}if(id==='fraction-products'&&mode===1)n+=d;const subtract=id==='fraction-sub';if(subtract&&mode===1){n+=d*2;m+=e;}if(subtract&&n*e<m*d){[n,m]=[m,n];[d,e]=[e,d];}const op=multiply?'×':subtract?'−':'+';const ans=multiply?fraction(n*m,d*e):fraction(n*e+(subtract?-1:1)*m*d,d*e);set(id==='fraction-area'?`Find the area of a rectangle with sides ${n}/${d} and ${m}/${e} units.`:kind==='fractionStory'?`A recipe uses ${n}/${d} cup. ${multiply?'Make '+m+'/'+e+' of the recipe. How much is needed?':'Add '+m+'/'+e+' cup more. How much altogether?'}`:'Calculate and simplify.','',`${F(n,d)} ${op} ${F(m,e)} = ${box}`,ans);}}
else if(kind==='decimalFraction'){const n=1+r(99);set(mode===0?'Write as a decimal.':mode===1?'Write as hundredths.':'Complete the equivalent fraction.','',mode===0?`${F(n,100)} = ${box}`:mode===1?`${(n/100).toFixed(2)} = ${box}/100`:`${F(a,10)} = ${box}/100`,mode===0?(n/100).toFixed(2):mode===1?n:a*10);}
else if(kind==='factors'){const n=2+r(99),factors=Array.from({length:n},(_,i)=>i+1).filter(x=>n%x===0);set(mode===0?`List the factor pairs of ${n}.`:mode===1?`Is ${n} prime or composite?`:`Is ${n} a multiple of ${a}?`,'',space,mode===0?factors.filter(x=>x<=n/x).map(x=>`${x} × ${n/x}`).join(', '):mode===1?(factors.length===2?'prime':'composite'):(n%a===0?'Yes':'No'));}
else if(kind==='elapsed'){const start=8*60+r(120),duration=1+r(89),end=start+duration;const fmt=m=>Math.floor(m/60)+':'+String(m%60).padStart(2,'0');set(mode===0?`Start at ${fmt(start)} a.m. Finish at ${fmt(end)} a.m. How many minutes?`:mode===1?`Start at ${fmt(start)} a.m. Work for ${duration} minutes. When do you finish?`:`Finish at ${fmt(end)} a.m. after ${duration} minutes. When did you start?`,'',box,mode===0?duration:mode===1?fmt(end)+' a.m.':fmt(start)+' a.m.');}
else if(kind==='convert'){const units=pick([['m','cm',100],['kg','g',1000],['L','mL',1000],['ft','in',12]]);const reverse=grade===5&&mode===1;set('Convert within the same measurement system.','',`${reverse?a*units[2]:a} ${reverse?units[1]:units[0]} = ${box} ${reverse?units[0]:units[1]}`,reverse?a:a*units[2]);}
else if(kind==='measureStory'){const unit=pick(['liters','grams']),sum=a*b;c=Math.min(c,sum);set(mode===1?`A container holds ${sum} ${unit}. Use ${c}. How much remains?`:`There are ${a} containers holding ${b} ${unit} each. How much altogether?`,'',box+' '+unit,mode===1?sum-c:sum);}
else if(kind==='graph'){const counts=[1+r(8),1+r(8),1+r(8)];visual=svg(counts.map((n,i)=>`<text x="5" y="${18+i*24}">${'ABC'[i]}</text>`+Array.from({length:n},(_,j)=>`<rect x="${25+j*18}" y="${5+i*24}" width="18" height="16" fill="#9dc2cb" stroke="white"/>`).join('')).join(''));set(mode===0?'Each block is one vote. How many votes for A?':mode===1?'How many votes altogether?':'How many more votes does the largest group have than the smallest?',visual,box,mode===0?counts[0]:mode===1?counts.reduce((a,b)=>a+b):Math.max(...counts)-Math.min(...counts));}
else if(kind==='lineplot'){const d=grade===2?1:grade===3?4:8,counts=[1+r(3),1+r(3),1+r(3),1+r(3)];visual=svg(counts.map((n,i)=>`<text x="${25+i*50}" y="75" text-anchor="middle" font-size="10">${fraction(i+1,d)}</text>`+Array.from({length:n},(_,j)=>`<text x="${25+i*50}" y="${55-j*17}" text-anchor="middle">×</text>`).join('')).join(''));set(mode===0?'Each × is one measurement. How many measurements?':mode===1?'How many measurements have the smallest shown value?':'What is the total of all the measured lengths?',visual,box,mode===0?counts.reduce((x,y)=>x+y):mode===1?counts[0]:fraction(counts.reduce((sum,n,i)=>sum+n*(i+1),0),d));}
else if(kind==='areaParts'||kind==='volumeParts'||kind==='cubes'){if(kind==='cubes'){const h=1+r(4);set(`A solid has ${h} layers. Each layer has ${a} rows of ${b} unit cubes. Find its volume.`,grid(a,b),box+' cubic units',a*b*h);}else if(kind==='volumeParts'){set(`Two non-overlapping rectangular prisms are joined. Their dimensions are ${a}×${b}×2 and ${c}×${b}×2. Find the total volume.`,'',box+' cubic units',(a+c)*b*2);}else if(id==='area-perimeter'){set(`A rectangle is ${a} units long and ${b} units wide. Find ${mode===1?'perimeter':'area'}.`,'',box,mode===1?2*(a+b):a*b);}else set(`A rectangle ${a+c} units long and ${b} units wide is split into widths ${a} and ${c}. Add the two areas.`,'',`${a} × ${b} + ${c} × ${b} = ${box}`,(a+c)*b);}
else if(kind==='expressions'){if(id==='write-expressions')set(`Add ${a} and ${b}, then multiply by ${c}. Write an expression without calculating.`,'',space,`(${a} + ${b}) × ${c}`);else set('Evaluate the grouped expression.','',mode===0?`(${a} + ${b}) × ${c} = ${box}`:mode===1?`${a} + (${b} × ${c}) = ${box}`:`[${a} + ${b}] × ${c} = ${box}`,mode===1?a+b*c:(a+b)*c);}
else if(kind==='paired'){const step=1+r(3),mult=2+r(3);if(id==='plot-points'){a=1+r(5);b=1+r(5);set(`Plot (${a}, ${b}) on the first-quadrant grid. Label the point P.`,svg(Array.from({length:7},(_,i)=>`<path d="M25 ${65-i*9}H79M${25+i*9} 11V65" stroke="#ccd8de"/><text x="${25+i*9}" y="78" font-size="7">${i}</text><text x="12" y="${67-i*9}" font-size="7">${i}</text>`).join('')+'<text x="90" y="68">x</text><text x="12" y="8">y</text>'),' ',`Point P is ${a} units right and ${b} units up from (0,0).`,true);}else set(`Both patterns start at 0. A adds ${step}; B adds ${step*mult}. Write the next three ordered pairs (A, B).`,'',space,`(${step}, ${step*mult}), (${step*2}, ${step*mult*2}), (${step*3}, ${step*mult*3}). B is ${mult} times A.`);}
else if(kind==='protractor'){const angle=pick([30,45,60,90,120,135,150]),rad=angle*Math.PI/180;const drawing=svg(`<path d="M65 65H180M65 65L${65+55*Math.cos(rad)} ${65-55*Math.sin(rad)}" stroke="#34576a" stroke-width="2"/>`,210,85);set(mode===2?`Use a protractor to draw an angle of ${angle}°.`:'Use a protractor to measure the angle.',mode===2?'':drawing,mode===2?space:box+' degrees',mode===2?`An angle of ${angle}°. Allow reasonable drawing tolerance.`:angle,mode===2);}
else if(kind==='lines'){const perpendicular=mode===1;set(mode===2?'Draw a line, a ray, and a line segment. Label each.':perpendicular?'Are these lines perpendicular?':'Are these lines parallel?',mode===2?'':svg(perpendicular?'<path d="M20 40H190M100 5V75" stroke="#34576a" stroke-width="2"/>':'<path d="M20 25H190M20 55H190" stroke="#34576a" stroke-width="2"/>'),mode===2?space:'Yes / No',mode===2?'Line: arrows at both ends. Ray: one endpoint and one arrow. Segment: two endpoints.':'Yes',mode===2);}
else if(kind==='symmetry'){const square=mode!==1;set('Draw all the lines of symmetry.',svg(`<rect x="60" y="10" width="${square?60:100}" height="60" fill="white" stroke="#34576a"/>`),'',square?'4 lines: vertical, horizontal, and both diagonals.':'2 lines: vertical and horizontal through the center.',true);}
else if(kind==='shape'||kind==='shapeClass'){const prompts=grade===2?[['How many sides does a pentagon have?','5'],['How many corners does a hexagon have?','6'],['How many square faces does a cube have?','6']]:grade===3?[['Is every rectangle a quadrilateral? Explain.','Yes. A rectangle has four sides.'],['Is a square a rectangle? Explain.','Yes. It has four right angles.'],['Draw a quadrilateral that is not a rectangle.','Example: a trapezoid with no right angles.']]:grade===4?[['A triangle has one 90° angle. What kind is it?','Right triangle.'],['A quadrilateral has two pairs of parallel sides. Name its family.','Parallelogram.'],['A triangle has angles 40°, 60°, 80°. Is it acute, right, or obtuse?','Acute: all angles are less than 90°.']]:[['Explain why every square is also a rectangle.','It has four right angles, so it satisfies the definition of a rectangle.'],['Is every rectangle a square? Explain.','No. A rectangle can have unequal adjacent side lengths.'],['Order these families from most specific to broadest: quadrilateral, square, rectangle.','Square → rectangle → quadrilateral.']];set(prompts[mode][0],'',space,prompts[mode][1],true);}
else throw Error('Unknown kind '+kind);
if(!prompt||answer==='')throw Error('Empty '+id);return{prompt,visual,task,answer,open};
};return [0,1,2].map(mode=>({questions:Array.from({length:mode===1?3:2},()=>make(mode))}));}
api.generate=extended;api.additions=additions;
if(typeof module!=='undefined')module.exports=api;
})(globalThis);
