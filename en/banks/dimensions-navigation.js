(()=>{const select=document.getElementById('unit');if(!select)return;const grade=Number(new URLSearchParams(location.search).get('grade'));const rows=grade===4?[['missing-side-area','Find a rectangle side from its area']]:grade===5?[['missing-dimension-volume','Find a prism height from its volume']]:[];if(!rows.length)return;const group=document.createElement('optgroup');group.label='Missing dimensions';for(const[id,title]of rows){const o=document.createElement('option');o.value='/en/'+id+'.html';o.textContent=title;group.append(o);}select.append(group);})();
