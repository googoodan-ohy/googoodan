const fs=require('fs'),vm=require('vm'),assert=require('assert/strict');const c={};vm.createContext(c);for(const p of ['ko/catalog.js','ko/engine.js','de/reuse/geometry-source.js','ko/art/catalog.js','ko/art.js','de/reuse/core-numbers.js','de/reuse/unit-catalog.js','de/reuse/time-units.js','ko/banks/measurement-targets.js','de/reuse/duration-units.js'])vm.runInContext(fs.readFileSync(p,'utf8'),c);let n=0;for(let seed=0;seed<100;seed++){const sections=c.KoMath.generate('de-by-time34',3,seed,3);for(let i=0;i<sections.length;i++)for(const q of sections[i].questions){assert(!/[가-힣]|undefined|NaN/.test(q.prompt+q.task+q.reason+q.answer));const {start,end,duration}=q.check;assert.equal(end-start,duration);if(i===0)assert.equal(q.answer,duration+' Minuten');else{const m=q.answer.match(/(\d+):(\d+)/);assert.equal(Number(m[1])*60+Number(m[2]),i===1?start:end);}n++;}}console.log('PASS '+n+' start/end/duration questions and answers');
