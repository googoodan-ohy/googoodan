const fs=require('fs'),vm=require('vm'),assert=require('assert/strict'),c={};vm.createContext(c);for(const p of ['ko/catalog.js','ko/engine.js','ko/art/catalog.js','ko/art.js','de/reuse/core-numbers.js','de/reuse/unit-catalog.js','ko/banks/data-targets.js','de/reuse/data-units.js'])vm.runInContext(fs.readFileSync(p,'utf8'),c);let n=0;for(let p=0;p<2;p++)for(let seed=0;seed<100;seed++)for(const s of c.KoMath.generate('de-by-data',p,seed,3))for(const q of s.questions){assert(!/[가-힣]|undefined|NaN/.test(q.prompt+q.visual+q.task+q.answer+q.reason));const v=q.check.values,sum=v.reduce((a,b)=>a+b,0);if(q.methodId==='picture-0')assert(q.answer.includes('insgesamt '+sum*10));if(q.methodId==='bar-0')assert(q.answer.endsWith(sum+' Stück'));if(q.methodId==='picture-2')assert.equal(parseInt(q.answer),(v[0]+v[1])*10);n++;}console.log('PASS '+n+' graph questions');
