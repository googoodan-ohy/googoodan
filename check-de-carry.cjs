const fs=require('fs'),vm=require('vm'),assert=require('assert/strict');const c={};vm.createContext(c);for(const p of ['ko/art/catalog.js','ko/art.js','de/reuse/core-numbers.js','de/reuse/unit-catalog.js','de/reuse/carry-source.js','de/reuse/carry-units.js'])vm.runInContext(fs.readFileSync(p,'utf8'),c);assert(fs.readFileSync('de/reuse/carry-source.js','utf8').includes(fs.readFileSync('ko/banks/arithmetic-targets.js','utf8')));let count=0;for(let p=0;p<7;p++)for(let seed=0;seed<50;seed++){const source=c.DeCarrySource.generate('3-1-1',[1,0,2,3,4,5,6][p],seed,3),out=c.KoMath.generate('de-by-carry-add',p,seed,3);out.forEach((s,i)=>s.questions.forEach((q,j)=>{const {a,b,value}=q.check;assert(a>=100&&a<=999&&b>=100&&b<=999);assert.equal(value,q.check.op==='+'?a+b:a-b);let x=a,y=b,carry=0,n=0;while(x||y){carry=q.check.op==='+'?(x%10+y%10+carry>=10?1:0):(x%10-carry<y%10?1:0);n+=carry;x=Math.floor(x/10);y=Math.floor(y/10);}assert.equal(n,q.check.target);assert.equal(q.answer,source[i].questions[j].answer);assert.equal(JSON.stringify(q.check),JSON.stringify(source[i].questions[j].check));assert(!/[가-힣]|undefined|NaN/.test(q.prompt+q.task+q.visual+q.reason+q.answer));count++;}));}console.log('PASS '+count+' German questions, specified carry/borrowing counts and unchanged original generator');
