const fs=require('fs'),vm=require('vm'),assert=require('assert/strict');const c={};vm.createContext(c);for(const p of ['ko/catalog.js','ko/engine.js','de/reuse/geometry-source.js','ko/art/catalog.js','ko/art.js','de/reuse/core-numbers.js','de/reuse/unit-catalog.js','de/reuse/arrays-units.js'])vm.runInContext(fs.readFileSync(p,'utf8'),c);let n=0;for(let seed=0;seed<100;seed++)for(const s of c.KoMath.generate('de-by-arrays12',0,seed,3))for(const q of s.questions){assert(!/[가-힣]|undefined|NaN/.test(q.prompt+q.task+q.reason+q.answer+q.visual));const {x,y,result}=q.check;assert(x>=2&&x<=6&&y>=2&&y<=9);assert.equal(result,x*y);assert.equal((q.visual.match(/<rect /g)||[]).length,result);if(q.mode!==2)assert.equal(q.answer,String(q.mode===1?y:result));else{const claim=Number(q.task.match(/Vorschlag: (\d+)/)[1]);assert.equal(q.answer,claim===result?'○':'×, Richtige Antwort: '+result);}n++;}console.log('PASS '+n+' German array questions and rectangle counts');
