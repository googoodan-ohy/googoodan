const fs=require('fs'),vm=require('vm'),assert=require('assert/strict');const c={};vm.createContext(c);for(const p of ['ko/art/catalog.js','ko/art.js','de/reuse/core-numbers.js','de/reuse/unit-catalog.js'])vm.runInContext(fs.readFileSync(p,'utf8'),c);let n=0;for(const id of ['de-by-numbers100','de-by-numbers999999'])for(let seed=0;seed<100;seed++){const a=c.DeCoreNumbers.generate(id==='de-by-numbers100'?'1-2-1':'by-sixdigits',seed,3),b=c.KoMath.generate(id,0,seed,3);for(let i=0;i<a.length;i++)for(let j=0;j<a[i].questions.length;j++){const original=a[i].questions[j],shown=b[i].questions[j];for(const field of ['prompt','answer','reason','task'])assert.equal(String(shown[field]).replace(/\u202f/g,''),String(original[field]));assert(shown.visual.replace(/\u202f/g,'').endsWith(original.visual));n++;}if(id==='de-by-numbers999999')assert(JSON.stringify(b).includes('\u202f'));}console.log('PASS '+n+' number questions match the underlying engine after removing grouping spaces');
