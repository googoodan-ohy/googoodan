const fs=require('fs'),vm=require('vm'),assert=require('assert/strict');const c={};vm.createContext(c);for(const p of ['ko/catalog.js','ko/engine.js','de/reuse/geometry-source.js','ko/art/catalog.js','ko/art.js','de/reuse/core-numbers.js','de/reuse/unit-catalog.js','de/reuse/countshapes-units.js'])vm.runInContext(fs.readFileSync(p,'utf8'),c);let n=0;for(let seed=0;seed<100;seed++)for(const s of c.KoMath.generate('de-by-countshapes12',0,seed,3))for(const q of s.questions){assert(!/[가-힣]|undefined|NaN/.test(q.prompt+q.task+q.reason+q.answer+q.visual));const symbol=q.prompt.match(/[●▲■]/)[0],expected=[...q.visual].filter(x=>x===symbol).length;assert(expected>=1&&expected<=6);if(q.mode!==2)assert.equal(q.answer,String(expected));else{const claim=Number(q.task.match(/Vorschlag: (\d+)/)[1]);assert.equal(q.answer,claim===expected?'○':'×, Richtige Antwort: '+expected);}n++;}console.log('PASS '+n+' German shape-count questions and picture counts');
