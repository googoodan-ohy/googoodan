const fs=require('fs'),vm=require('vm'),assert=require('assert/strict');const c={};vm.createContext(c);for(const p of ['ko/catalog.js','ko/engine.js','ko/art/catalog.js','ko/art.js','ko/banks/reasoning-targets.js','en/types.js','en/banks/dimensions.js'])vm.runInContext(fs.readFileSync(p,'utf8'),c);let total=0;for(const[id,,skills]of c.DimensionPractice.defs)for(const skill of skills)for(let seed=0;seed<100;seed++){c.DimensionPractice.select(skill);for(const s of c.KoMath.generate('dimensions:'+id,0,seed,1)){const q=s.questions[0];assert(!/[가-힣]|undefined|NaN/.test(q.prompt+q.task+q.reason+q.answer));const[a,b,d]=q.prompt.match(/\d+(?:\.\d+)?/g).map(Number),x=parseFloat(q.answer);if(skill==='area-inverse-0'||skill==='area-inverse-2')assert.equal(x*b,a);if(skill==='area-inverse-1')assert.equal(x*b/2,a);if(skill==='volume-inverse-0')assert.equal(a*b*x,d);if(skill==='volume-inverse-1')assert.equal(2*(a*b+a*x+b*x),d);if(skill==='volume-inverse-2')assert.equal(6*x*x,a);total++;}}
console.log('PASS '+total+' independently checked dimension questions');
