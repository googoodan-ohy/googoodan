const fs=require('fs'),vm=require('vm'),assert=require('assert/strict');const c={};vm.createContext(c);for(const f of ['ko/catalog.js','ko/engine.js','de/reuse/geometry-source.js','ko/art/catalog.js','ko/art.js','de/reuse/core-numbers.js','de/reuse/unit-catalog.js','de/reuse/compare-units.js'])vm.runInContext(fs.readFileSync(f,'utf8'),c);let n=0;for(let p=0;p<2;p++)for(let seed=0;seed<100;seed++){const src=c.DeGeometrySource.generate('1-1-4',0,seed,3,[0,1,2].map(mode=>({skill:['length-compare','area-compare'][p],mode})));c.KoMath.generate('de-by-compare12',p,seed,3).forEach((s,i)=>s.questions.forEach((q,j)=>{assert.equal(String(q.answer),String(src[i].questions[j].answer).replaceAll('바른 답:','Richtige Antwort:').replaceAll('가','A').replaceAll('나','B'));assert(!/[가-힣]|undefined|NaN/.test(q.prompt+q.task+q.visual+q.answer+q.reason));const widths=[...q.visual.matchAll(/<rect[^>]*width="(\d+)"/g)].map(m=>+m[1]);assert.equal(widths.length,2);assert.notEqual(widths[0],widths[1]);if(q.mode===0)assert.equal(q.answer,widths[0]>widths[1]?'A':'B');n++;}));}console.log('PASS '+n+' comparison questions and graphical answers');