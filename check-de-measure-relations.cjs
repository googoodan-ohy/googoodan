const fs=require('fs'),vm=require('vm'),assert=require('assert/strict');const c={};vm.createContext(c);for(const p of ['ko/catalog.js','ko/engine.js','de/reuse/geometry-source.js','ko/art/catalog.js','ko/art.js','de/reuse/core-numbers.js','de/reuse/unit-catalog.js','ko/banks/measurement-targets.js','de/reuse/measure-relations.js'])vm.runInContext(fs.readFileSync(p,'utf8'),c);let n=0;for(let p=0;p<5;p++)for(let seed=0;seed<100;seed++){const sections=c.KoMath.generate('de-by-measure-relations',p,seed,3);for(let i=0;i<sections.length;i++)for(const q of sections[i].questions){assert(!/[가-힣]|undefined|NaN/.test(q.prompt+q.task+q.reason+q.answer));const {a,b,total,factor}=q.check;assert.equal(total,a*factor+b);if(i===0)assert.equal(Number(q.answer.match(/\d+/)[0]),total);if(i===1)assert.deepEqual(q.answer.match(/\d+/g).map(Number),[a,b]);if(i===2){const other=Number(q.task.match(/\d+/g).at(-1));assert.equal(q.answer,total===other?'=':total>other?'>':'<');}n++;}}console.log('PASS '+n+' measurement conversions, reverse conversions and comparisons');
