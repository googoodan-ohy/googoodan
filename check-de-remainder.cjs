const fs=require('fs'),vm=require('vm'),assert=require('assert/strict');const c={};vm.createContext(c);for(const p of ['ko/catalog.js','ko/engine.js','de/reuse/geometry-source.js','ko/art/catalog.js','ko/art.js','de/reuse/core-numbers.js','de/reuse/unit-catalog.js','de/reuse/remainder-units.js'])vm.runInContext(fs.readFileSync(p,'utf8'),c);let n=0;for(let seed=0;seed<100;seed++)for(const s of c.KoMath.generate('de-by-remainder',0,seed,3))for(const q of s.questions){assert(!/[가-힣]|undefined|NaN/.test(q.prompt+q.task+q.reason+q.answer+q.visual));const {total,divisor,quotient,remainder}=q.check;assert.equal(total,divisor*quotient+remainder);assert(remainder>0&&remainder<divisor);if(q.mode===0)assert.equal(q.answer,quotient+' Rest '+remainder);n++;}console.log('PASS '+n+' remainder questions');
