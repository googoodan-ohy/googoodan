const fs=require('fs'),vm=require('vm'),assert=require('assert/strict');const c={};vm.createContext(c);for(const p of ['ko/catalog.js','ko/engine.js','de/reuse/geometry-source.js','ko/art/catalog.js','ko/art.js','de/reuse/core-numbers.js','de/reuse/unit-catalog.js','de/reuse/buildings-units.js'])vm.runInContext(fs.readFileSync(p,'utf8'),c);let n=0;const tr=s=>String(s).replaceAll('바른 답:','Richtige Antwort:');for(let seed=0;seed<100;seed++){const src=c.DeGeometrySource.generate('6-2-3',0,seed,3,[0,1,2].map(mode=>({skill:'cube-view',mode})));const out=c.KoMath.generate('de-by-buildings',2,seed,3);out.forEach((s,i)=>s.questions.forEach((q,j)=>{const original=src[i].questions[j];assert.equal(String(q.answer),tr(original.answer));assert(!/[가-힣]|undefined|NaN/.test(q.prompt+q.visual+q.answer+q.reason+q.task));const hs=[...q.visual.matchAll(/<span>(\d+)<\/span>/g)].map(m=>+m[1]);assert.equal(hs.length,4);assert.equal(q.prompt.includes('von vorne'),original.prompt.startsWith('앞'));if(q.mode===0){const expected=q.prompt.includes('von vorne')?Math.max(hs[0],hs[2])+Math.max(hs[1],hs[3]):Math.max(hs[0],hs[1])+Math.max(hs[2],hs[3]);assert.equal(+q.answer,expected);}n++;}));}console.log('PASS '+n+' building-view questions and source answers');
