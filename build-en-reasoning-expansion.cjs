const fs=require('fs'),vm=require('vm');
const sources=['ko/catalog.js','ko/engine.js','ko/art/catalog.js','ko/art.js','ko/banks/multiplication.js','ko/banks/early-numbers.js','ko/banks/core.js','ko/banks/fractions.js','ko/banks/early-arithmetic.js','ko/question-engine.js','ko/banks/fraction-targets.js','en/types.js','en/banks/reasoning-expansion.js'];
const ctx={URLSearchParams};vm.createContext(ctx);for(const p of sources)vm.runInContext(fs.readFileSync(p,'utf8'),ctx);
const defs=JSON.parse(JSON.stringify(ctx.ReasoningExpansion.defs)),labels=JSON.parse(JSON.stringify(ctx.ReasoningExpansion.labels));
const version='20260911-reasoning-expansion',template=fs.readFileSync('en/fraction-models.html','utf8');
const descriptions={
 'equivalent-fraction-puzzles':'Free printable equivalent fraction puzzles. Find missing numerators and solve denominator-limit and numerator-sum challenges with answer keys.',
 'fraction-number-puzzles':'Free printable fraction number puzzles. List simplest fractions, find fractions between bounds and work backward from clues with answer keys.',
 'fraction-missing-numbers':'Free printable missing number fraction worksheets. Find unknown addends, factors and dividends, including mixed numbers, with answer keys.',
 'fraction-error-analysis':'Free printable fraction error analysis worksheets. Check proposed answers for addition, subtraction, multiplication and division, then correct mistakes.',
 'multiplication-arrays':'Free printable multiplication arrays and equal groups worksheets. Read picture models, write equations and explore commutative pairs with answers.',
 'multiplication-strategies':'Free printable multiplication strategies worksheets. Use repeated addition, number-line jumps, known facts and patterns with matching answer keys.',
 'multiplication-missing-factors':'Free printable missing factor multiplication worksheets. Find unknown factors, complete fact tables and choose number cards to reach a target.',
 'multiplication-error-analysis':'Free printable multiplication error analysis worksheets. Find an incorrect product, write a corrected equation and explain your thinking.',
 'multiplication-matching':'Free printable multiplication matching worksheets. Connect each equation to its product, change the numbers and print a matching answer key.',
 'draw-multiplication-groups':'Free printable draw equal groups worksheets. Represent multiplication with dots or pictures and check group sizes against the answer key.'
};
const notes={
 'equivalent-fraction-puzzles':'The denominator-limit and sum clues are enrichment tasks. Begin with equivalent fractions before adding these constraints.',
 'fraction-number-puzzles':'These enrichment tasks require understanding proper fractions, common factors and equivalent fractions. They are not introductory fraction lessons.',
 'fraction-missing-numbers':'Addition, subtraction and multiplication support Grade 5 practice. General fraction division is provided for Grade 6. Entry links from Grade 5 exclude division.',
 'fraction-error-analysis':'Some proposed answers are correct and some are incorrect. Recalculate before deciding. Grade 5 entry links exclude general fraction division.',
 'multiplication-arrays':'Read the direction carefully: these sheets write items per group multiplied by number of groups. Both factor orders give the same total. The array activity also asks for both equations.',
 'draw-multiplication-groups':'Many drawings can be correct. Check the number of groups, the items in each group and the total. Students may use dots instead of detailed pictures.'
};
const links=defs.map(([id,title])=>`<li><a href="/en/${id}.html">${title}</a></li>`).join(''),paths=[];
for(const[id,title,skills,level]of defs){
 const url='https://googoodan.com/en/'+id+'.html',description=descriptions[id];let html=template.replaceAll('Fraction Models Worksheets',title).replaceAll('fraction-models.html',id+'.html').replace('data-type="fraction-models"',`data-type="${id}"`);
 html=html.replace(/<script[^>]*src="(?:\/ko\/(?:catalog|engine)|\/en\/banks\/coverage(?:-controls)?)\.js[^>]*><\/script>/g,'');
 html=html.replace(/(<script defer src="app\.js)/,sources.filter(p=>p!=='en/types.js').map(p=>`<script defer src="/${p}?v=${version}"></script>`).join('')+'$1');
 html=html.replace('</head>',`<link rel="stylesheet" href="/en/banks/reasoning-expansion.css?v=${version}"><script defer src="/en/banks/reasoning-expansion-controls.js?v=${version}"></script></head>`);
 html=html.replace(/<meta (?:name="description"|property="og:description") content="[^"]*">/g,m=>m.replace(/content="[^"]*"/,`content="${description}"`));
 html=html.replace(/<script type="application\/ld\+json">([\s\S]*?)<\/script>/g,(all,json)=>{const d=JSON.parse(json);if(d['@type']==='LearningResource'){Object.assign(d,{name:title,description,url,educationalLevel:level});return'<script type="application/ld+json">'+JSON.stringify(d)+'</script>';}return all;});
 html=html.replace('<footer class="paper-footer">','<div class="reasoning-art-credit">Pictures: Twemoji, © Twitter and contributors · CC BY 4.0</div><footer class="paper-footer">');
 const body=`<section class="seo-resource" aria-labelledby="resource-heading"><h2 id="resource-heading">${title}: printable practice</h2><p id="resource-description">${description}</p><p>${level}. ${notes[id]||'Choose a focused activity to practice one strategy, or use the mixed sheet to connect related ideas.'}</p><h3>Available activities</h3><ul>${skills.map(s=>'<li>'+labels[s]+'</li>').join('')}</ul><h3>Use the worksheet</h3><p>Choose a practice type, read each question and show your work in the space provided. The mixed sheet contains four tasks; a focused sheet repeats the selected activity with different questions. Use New problems to generate another set.</p><p>Switch to Answer key to check the same questions. Select the worksheet, the answer key, or both in the print controls. You can print on A4 or US Letter paper, or save a PDF using your browser print dialog. The worksheets are free for home and classroom practice.</p><h3>Explain and check</h3><p>Ask the learner to describe the relationship in the question before calculating. For a missing number, substitute the answer back into the original equation. For a picture, count groups and items consistently. When a proposed answer is wrong, explain the correction instead of only crossing it out.</p><p>These are selected practice and enrichment activities, not a complete curriculum. Use a simpler related worksheet if the learner has not met the prerequisite concept. Keep short practice sessions focused on understanding as well as accuracy.</p><p>Artwork: Twemoji, © Twitter and contributors, <a href="https://creativecommons.org/licenses/by/4.0/">CC BY 4.0</a>.</p><p id="resource-example">Choose an activity and preview the questions above.</p><p id="resource-instruction">Show your work, check the answer and explain your strategy.</p><h3>Related worksheets</h3><ul id="resource-related">${links}</ul></section>`;
 html=html.replace(/<section class="seo-resource"[\s\S]*?<\/section>/,body);fs.writeFileSync('en/'+id+'.html',html);paths.push('en/'+id+'.html');
}
const add=(p,section)=>{let h=fs.readFileSync(p,'utf8');h=h.includes('id="reasoning-expansion-links"')?h.replace(/<section id="reasoning-expansion-links">[\s\S]*?<\/section>/,section):h.replace('</main>',section+'</main>');fs.writeFileSync(p,h);paths.push(p);};
for(const p of ['en/index.html','en/worksheets.html'])add(p,`<section id="reasoning-expansion-links"><h2>Fraction reasoning and multiplication activities</h2><ul>${links}</ul></section>`);
for(const grade of [3,4,5,6]){const rows=defs.filter(d=>grade<=4?d[0].startsWith('multiplication-')||d[0]==='draw-multiplication-groups':d[0].startsWith('fraction-')||d[0]==='equivalent-fraction-puzzles');add('en/grade-'+grade+'.html',`<section id="reasoning-expansion-links"><h2>${grade===4?'Multiplication review':'More reasoning practice'}</h2><ul>${rows.map(([id,title])=>`<li><a href="/en/${id}.html?grade=${grade}">${title}</a></li>`).join('')}</ul></section>`);}
let g=fs.readFileSync('en/grades.html','utf8');if(!g.includes('reasoning-expansion-navigation.js'))g=g.replace('</body>',`<script defer src="/en/banks/reasoning-expansion-navigation.js?v=${version}"></script></body>`);fs.writeFileSync('en/grades.html',g);paths.push('en/grades.html');
let sm=fs.readFileSync('sitemap.xml','utf8');for(const[id]of defs){const url='https://googoodan.com/en/'+id+'.html';if(!sm.includes('<loc>'+url+'</loc>'))sm=sm.replace('</urlset>',`<url><loc>${url}</loc></url>\n</urlset>`);}fs.writeFileSync('sitemap.xml',sm);paths.push('sitemap.xml');
fs.mkdirSync('.work-english/reasoning-expansion',{recursive:true});fs.writeFileSync('.work-english/reasoning-expansion/manifest.json',JSON.stringify({defs,sources,paths,version},null,2));console.log('Built '+defs.length+' pages and '+defs.reduce((sum,d)=>sum+d[2].length,0)+' selectable activity variants');
