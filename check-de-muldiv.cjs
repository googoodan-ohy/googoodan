const fs=require('fs'),vm=require('vm'),assert=require('assert/strict');const c={};vm.createContext(c);for(const p of ['en/types.js','de/reuse/drill-catalog.js','de/reuse/drill-products.js','de/reuse/drill-engine.js'])vm.runInContext(fs.readFileSync(p,'utf8'),c);let n=0;const u=c.DrillCatalog.units.find(x=>x.id==='de-by-muldiv');for(const p of u.drills)for(let seed=0;seed<30;seed++)for(const q of c.DrillEngine.rows(p,seed,20)){assert(!/[가-힣]|undefined|NaN/.test(JSON.stringify(q)));if(q.op){assert.equal(+q.answer,q.op==='×'?q.a*q.b:q.a/q.b);assert(Math.max(q.a,q.b,+q.answer)<=90);assert(Math.min(q.a,q.b,+q.answer)>=0);}else{const m=q.work.match(/^(\d+) ([×÷]) (\d+) = (\d+)$/);assert(m,JSON.stringify(q));assert.equal(+m[4],m[2]==='×'?+m[1]* +m[3]:+m[1]/ +m[3]);assert.equal(+q.answer,+m[4]);}n++;}console.log('PASS '+n+' upper drill questions');
