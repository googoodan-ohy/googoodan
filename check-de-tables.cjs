const fs=require('fs'),vm=require('vm'),assert=require('assert/strict');const c={};vm.createContext(c);for(const p of ['ko/catalog.js','ko/engine.js','de/reuse/geometry-source.js','ko/art/catalog.js','ko/art.js','de/reuse/core-numbers.js','de/reuse/unit-catalog.js','de/reuse/tables-units.js'])vm.runInContext(fs.readFileSync(p,'utf8'),c);let n=0;for(let seed=0;seed<100;seed++)for(const s of c.KoMath.generate('de-by-tables',0,seed,3))for(const q of s.questions){assert(!/[가-힣]|undefined|NaN/.test(q.prompt+q.task+q.reason+q.answer+q.visual));const vals=[...q.visual.matchAll(/<td>(\d+)<\/td>/g)].map(m=>Number(m[1])),factor=vals[4];assert.deepEqual(vals.slice(0,4),[1,2,3,4]);assert(factor>=2&&factor<=5);assert.equal(vals[5],factor*2);assert.equal(vals[6],factor*3);const expected=factor*4;if(q.mode!==2)assert.equal(q.answer,String(expected));else{const claim=Number(q.task.match(/Vorschlag: (\d+)/)[1]);assert.equal(q.answer,claim===expected?'○':'×, Richtige Antwort: '+expected);}n++;}console.log('PASS '+n+' German multiplication-table questions and answers');
