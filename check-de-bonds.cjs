const fs=require('fs'),vm=require('vm'),assert=require('assert/strict');const c={};vm.createContext(c);for(const p of ['ko/catalog.js','ko/engine.js','de/reuse/geometry-source.js','ko/art/catalog.js','ko/art.js','de/reuse/core-numbers.js','de/reuse/unit-catalog.js','de/reuse/bonds-units.js'])vm.runInContext(fs.readFileSync(p,'utf8'),c);let n=0;for(let profile=0;profile<2;profile++)for(let seed=0;seed<100;seed++)for(const s of c.KoMath.generate('de-by-bonds12',profile,seed,3))for(const q of s.questions){assert(!/[가-힣]|undefined|NaN/.test(q.prompt+q.task+q.reason+q.answer+q.visual));const m=q.prompt.match(/Zerlege (\d+) in (\d+)/),total=Number(m[1]),part=Number(m[2]),expected=total-part;assert(total<= (profile===0?10:20));assert(expected>0);if(profile===0)assert.equal((q.visual.match(/<circle /g)||[]).length,total);if(q.mode!==2)assert.equal(q.answer,String(expected));else{const claim=Number(q.task.match(/Vorschlag: (\d+)/)[1]);assert.equal(q.answer,claim===expected?'○':'×, Richtige Antwort: '+expected);}n++;}console.log('PASS '+n+' German number-bond questions and dot diagrams');
