const fs=require('fs'),vm=require('vm'),assert=require('assert/strict');
const load=(c,files)=>files.forEach(f=>vm.runInContext(fs.readFileSync(f,'utf8'),c));
const c={};vm.createContext(c);load(c,['ko/catalog.js','ko/engine.js','it/reuse/unit-catalog.js']);
const tr=s=>String(s).replaceAll('불가능하다','impossibile').replaceAll('확실하다','certo').replaceAll('반반이다','probabilità del 50%').replaceAll('삼각형','triangolo').replaceAll('사각형','quadrilatero').replaceAll('원','cerchio').replaceAll('선분','segmento').replaceAll('반직선','semiretta').replaceAll('직선','retta').replace(/(\d+)시\s*(\d+)분/g,(_,h,m)=>h+':'+m.padStart(2,'0')).replaceAll('바른 답:','Risposta corretta:').replaceAll('예각','acuto').replaceAll('직각','retto').replaceAll('둔각','ottuso').replaceAll('아니요','No').replaceAll('예','Sì').replace(/(\d)\.(\d)/g,'$1,$2');let count=0;
for(const[id,g,n,name,src,skill]of c.ItDefinitions)for(let seed=0;seed<30;seed++){
const a=c.KoMath.generate(id,0,seed,3),b=c.ItSourceMath.generate(src,0,seed,3,[0,1,2].map(mode=>({skill,mode})));
a.forEach((s,i)=>s.questions.forEach((q,j)=>{assert.equal(q.answer,tr(b[i].questions[j].answer));assert.equal(JSON.stringify(q.check),JSON.stringify(b[i].questions[j].check));if(q.check?.kind==='calc'&&i===2)assert(q.prompt.includes(q.check.op),'Check task must show the operation');assert(!/[가-힣]|undefined|NaN/.test(q.prompt+q.task+q.reason+q.visual+q.answer));count++;}));}
const d={};vm.createContext(d);load(d,['en/types.js','it/reuse/drill-catalog.js','ko/drill-engine.js']);const rows=d.DrillEngine.rows;load(d,['it/reuse/drill-localize.js']);let drills=0;
for(const p of d.DrillCatalog.units.flatMap(u=>u.drills))for(let seed=0;seed<40;seed++){
const a=d.DrillEngine.rows(p,seed,4),b=rows(p,seed,4);a.forEach((q,i)=>{if(String(b[i].answer).includes('묶음'))assert.deepEqual(String(q.answer).match(/\d+/g),String(b[i].answer).match(/\d+/g));else assert.equal(q.answer,b[i].answer);assert.equal(q.work,typeof b[i].work==='string'?b[i].work.replaceAll(' 나머지 ',' resto '):b[i].work);for(const k of ['a','b','op','mask'])assert.equal(q[k],b[i][k]);assert(!/[가-힣]|undefined|NaN/.test((q.prompt||'')+(q.work||'')+q.answer));drills++;});}
console.log('PASS '+count+' activity answers and '+drills+' drill questions against the original Korean generators');
