const fs=require('fs'),vm=require('vm'),assert=require('assert/strict');const c={};vm.createContext(c);for(const p of ['en/types.js','de/reuse/drill-catalog.js','de/reuse/drill-engine.js'])vm.runInContext(fs.readFileSync(p,'utf8'),c);let n=0;for(const source of ['multiply-one','divide'])for(let seed=0;seed<100;seed++){const p={id:'test',grade:3,mode:'story',pool:[{source}]};for(const q of c.DrillEngine.rows(p,seed,20)){assert(!/[가-힣]|Une quantité|Calcule|Trouve|Quel nombre|undefined|NaN/.test(q.prompt));const m=q.work.match(/^(\d+) ([×÷]) (\d+) = (\d+)$/);assert(m,JSON.stringify(q));assert.equal(+m[4],m[2]==='×'?+m[1]* +m[3]:+m[1]/ +m[3]);assert.equal(+q.answer,+m[4]);n++;}}console.log('PASS '+n+' German multiplication/division stories');
