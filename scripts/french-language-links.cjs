const fs=require('fs'),path=require('path');const root=path.resolve(__dirname,'..');let changed=0,replaced=0;for(const f of fs.readdirSync(path.join(root,'fr'),{recursive:true}).filter(f=>f.endsWith('.html'))){const p=path.join(root,'fr',f),old=fs.readFileSync(p,'utf8');const s=old.replace(/<a\b[^>]*href=["']\/["'][^>]*>[\s\S]*?<\/a>/g,m=>{if(!/한국어/.test(m))return m;replaced++;return m.replace(/href=(["'])\/\1/,'href="/ko/"');});if(s!==old){fs.writeFileSync(p,s);changed++;}}console.log({changed,replaced});
