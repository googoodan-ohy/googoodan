const fs=require('fs'),vm=require('vm'),assert=require('assert/strict');const c={};vm.createContext(c);for(const p of ['ko/catalog.js','ko/engine.js','de/reuse/geometry-source.js','ko/art/catalog.js','ko/art.js','de/reuse/core-numbers.js','de/reuse/unit-catalog.js','de/reuse/buildings-units.js'])vm.runInContext(fs.readFileSync(p,'utf8'),c);let n=0;for(let p=0;p<2;p++)for(let seed=0;seed<100;seed++)for(const s of c.KoMath.generate('de-by-buildings',p,seed,3))for(const q of s.questions){assert(!/[가-힣]|undefined|NaN/.test(q.prompt+q.task+q.reason+q.answer+q.visual),JSON.stringify(q));if(q.mode===0){const values=[...q.visual.matchAll(/<span>(\d+)<\/span>/g)].map(m=>+m[1]);assert.equal(values.length,4);assert.equal(+q.answer,values.slice(p).reduce((a,b)=>a+b,0));}n++;}console.log('PASS '+n+' localized building questions and diagram answers');
