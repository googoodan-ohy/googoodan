const fs=require('fs'),vm=require('vm'),assert=require('assert/strict');const c={};vm.createContext(c);for(const p of ['ko/catalog.js','ko/engine.js','de/reuse/geometry-source.js','ko/art/catalog.js','ko/art.js','de/reuse/core-numbers.js','de/reuse/unit-catalog.js','ko/banks/measurement-targets.js','de/reuse/clockdraw-units.js'])vm.runInContext(fs.readFileSync(p,'utf8'),c);let n=0;for(let p=0;p<2;p++)for(let seed=0;seed<100;seed++){const sections=c.KoMath.generate('de-by-clockdraw12',p,seed,3);for(let i=0;i<sections.length;i++)for(const q of sections[i].questions){assert(!/[가-힣]|undefined|NaN/.test(q.prompt+q.task+q.reason+q.answer+q.visual));const times=[...q.answer.matchAll(/(\d+):(\d+)/g)].map(m=>[Number(m[1]),Number(m[2])]),[h,m]=times[0];assert.equal(m%(p===0?30:5),0);const paths=[...q.visual.matchAll(/<path d="M70 70L([^ ]+) ([^"]+)"/g)];assert.equal(paths.length,i===1?2:0);if(i===1){assert.equal(times[1][0]*60+times[1][1],h*60+m+30);for(const [j,angle,length]of [[0,(h%12+m/60)*Math.PI/6,29],[1,m*Math.PI/30,42]]){assert(Math.abs(Number(paths[j][1])-(70+length*Math.sin(angle)))<1e-8);assert(Math.abs(Number(paths[j][2])-(70-length*Math.cos(angle)))<1e-8);}}n++;}}console.log('PASS '+n+' clock drawing prompts, hands and 30-minute answers');
