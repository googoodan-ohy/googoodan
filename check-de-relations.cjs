const fs=require('fs'),vm=require('vm'),assert=require('assert/strict');const c={};vm.createContext(c);for(const p of ['ko/catalog.js','ko/engine.js','de/reuse/geometry-source.js','ko/art/catalog.js','ko/art.js','de/reuse/core-numbers.js','de/reuse/unit-catalog.js','de/reuse/relations-units.js'])vm.runInContext(fs.readFileSync(p,'utf8'),c);let n=0;for(let p=0;p<2;p++)for(let seed=0;seed<100;seed++)for(const s of c.KoMath.generate('de-by-relations',p,seed,3))for(const q of s.questions){assert(!/[가-힣]|undefined|NaN/.test(q.prompt+q.task+q.reason+q.answer+q.visual));const a=Number(q.prompt.match(/\d+/)[0]),expected=p===0?Array.from({length:a},(_,i)=>i+1).filter(d=>a%d===0).join(', '):[a,a*2,a*3].join(', ');if(q.mode!==2)assert.equal(q.answer,expected);else{const claim=q.task.match(/Vorschlag: ([^<]+)/)[1];assert.equal(q.answer,claim===expected?'○':'×, Richtige Antwort: '+expected);}n++;}console.log('PASS '+n+' German factor/multiple questions and answers');
