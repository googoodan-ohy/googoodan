const fs=require('fs'),vm=require('vm'),assert=require('assert/strict');const c={};vm.createContext(c);for(const p of ['ko/art/catalog.js','ko/art.js','de/reuse/core-numbers.js','de/reuse/unit-catalog.js','ko/banks/early-arithmetic.js','de/reuse/calc100strategies-units.js'])vm.runInContext(fs.readFileSync(p,'utf8'),c);let count=0;for(let profile=0;profile<2;profile++)for(let seed=0;seed<100;seed++){const src=c.KoEarlyArithmetic.generate('1-2-6',[1,2][profile],seed,3,[]),out=c.KoMath.generate('de-by-calc100strategies12',profile,seed,3);out.forEach((s,i)=>s.questions.forEach((q,j)=>{const before=src[i].questions[j],nums=(q.visual.replace(/<[^>]*>/g,' ').match(/\d+/g)||[]).map(Number),ans=q.answer.match(/\d+/g).map(Number);assert.equal(q.artId,before.artId);assert.equal(JSON.stringify(q.check),JSON.stringify(before.check));assert.equal(q.answer,before.answer.replace('예:','Beispiel:'));assert(!/[가-힣]|undefined|NaN/.test(q.prompt+q.visual+q.task+q.reason+q.answer));assert(q.check.a+q.check.b<=100);switch(q.methodId){case 'table':assert.equal(ans[0],nums[0]+nums[1]);assert.equal(ans[1],nums[2]+nums[3]);break;case 'story':assert.equal(ans[0]+ans[1],ans[2]);break;case 'missing-add':assert.equal(ans[0],q.check.a);break;case 'inverse':assert.equal(ans[0]+ans[1],ans[2]);assert.equal(ans[3]-ans[4],ans[5]);break;case 'compensate':assert.equal(nums[0]+nums[1],nums[2]+ans[0]);break;case 'error':assert.equal(ans[0],q.check.a+q.check.b);break;}count++;}));}console.log('PASS '+count+' German calculation strategy questions within 100');
