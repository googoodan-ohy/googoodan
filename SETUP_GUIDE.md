# 구구단닷컴 자동 문제지 생성 시스템 - 셋업 가이드

## ✅ 압축을 풀면 이미 다 준비되어 있어요

이 zip 하나에 필요한 모든 것이 들어있습니다. 별도로 파일을 옮기거나 배치할 필요가 없어요.

```
구구단닷컴\                    ← 압축 풀면 생기는 폴더
├── server.js                  ← 백엔드 서버
├── package.json               ← 의존성 목록
├── index_main.html            ← 메인 페이지
├── SETUP_GUIDE.md             ← 이 문서
└── work\                      ← 학년별 문제지 폴더 (71개 단원 전부 구성됨)
    ├── 1학년\
    │   ├── 1학기\
    │   │   ├── 1단원 9까지의 수\
    │   │   │   └── problem1.html
    │   │   ├── 2단원 여러 가지 모양\
    │   │   │   └── problem1.html
    │   │   └── ...
    │   └── 2학기\
    ├── 2학년\
    └── ... (6학년까지)
```

`server.js`는 자기 자신이 있는 위치를 기준으로 `work` 폴더를 자동으로 찾도록 되어 있어서, **이 폴더를 어디에 두든(바탕화면, C드라이브, G드라이브 등) 그대로 작동**합니다.

---

## ⚙️ 실행 방법 (3단계)

### 1️⃣ Node.js 설치 (최초 1회만)
- https://nodejs.org/ 에서 LTS 버전 다운로드 및 설치
- 설치 후 명령 프롬프트(또는 PowerShell)에서 `node --version` 입력해 확인

### 2️⃣ 압축 풀기
- 전달받은 zip 파일을 원하는 위치에 압축 해제
- 예: `C:\구구단닷컴\` 또는 바탕화면 등 아무 곳이나 가능

### 3️⃣ 서버 실행
압축을 푼 `구구단닷컴` 폴더로 들어가서, 그 폴더 안에서 명령 프롬프트를 열고:

```powershell
npm install
npm start
```

`npm install`은 처음 한 번만 실행하면 되고, 그다음부터는 `npm start`만 실행하면 됩니다.

예상 출력:
```
✓ 서버 시작: http://localhost:3000
✓ 작업 폴더: C:\구구단닷컴\work
✓ API 테스트: http://localhost:3000/api/structure
```

### 폴더 안에서 명령 프롬프트 여는 방법
탐색기에서 `구구단닷컴` 폴더로 들어간 뒤, 주소창에 `cmd` 입력하고 엔터 → 그 위치에서 명령 프롬프트가 열립니다.

---

## 🌐 메인 페이지 열기

서버가 실행 중인 상태에서 브라우저 주소창에:
```
http://localhost:3000/index_main.html
```

⚠️ `index_main.html` 파일을 더블클릭해서 직접 열면 안 됩니다. 반드시 서버를 통해(`http://localhost:3000/...`) 접속해야 폴더 스캔 API가 작동합니다.

---

## 🎯 사용 흐름

1. **서버 실행** → `npm start` (터미널을 계속 켜둬야 함)
2. **메인 페이지 열기** → `http://localhost:3000/index_main.html`
3. **학년·학기·단원 선택** → 드롭다운에서 선택
4. **문제지 파일 선택** → 버튼 클릭
5. **문제지 미리보기** → 화면에 표시
6. **버튼 사용**:
   - 📄 **문제지** / **정답지** — 모드 토글
   - 🔄 **숫자변경** — 새로운 난수로 문제 재생성
   - 🖨️ **프린트** — 인쇄 또는 PDF 저장

---

## 📝 새로운 문제지 추가하는 방법

이미 71개 단원 폴더가 다 만들어져 있어서, 각 단원에 문제지 파일을 추가만 하면 됩니다.

1. 해당 단원 폴더로 이동 (예: `work\1학년\1학기\1단원 9까지의 수\`)
2. 그 폴더 안의 `problem1.html`을 복사해서 `problem2.html`로 이름 바꾸기
3. 파일을 열어서 아래 CONFIG 부분만 수정:
   ```javascript
   const CONFIG = {
     grade: 1,
     semester: 1,
     unitName: '9까지의 수',
     practiceType: '연산연습',   // '연산연습' / '개념연습' / '유형연습'
     setNum: 2                   // 세트 번호를 2로
   };
   ```
4. 저장 후 **메인 페이지 새로고침 (Ctrl+F5)** — 자동으로 메뉴에 추가됩니다!

새 단원을 통째로 추가하고 싶으면, `work\학년\학기\` 안에 새 폴더(예: `6단원 규칙 찾기`)를 만들고 그 안에 problem.html을 넣으면 됩니다. 폴더명 맨 앞의 숫자(1단원, 2단원...)는 표시 순서에는 영향 없고 구분용입니다.

---

## 🔍 API 엔드포인트 (참고용)

| 엔드포인트 | 설명 |
|-----------|------|
| `GET /api/structure` | 전체 폴더 구조 JSON 반환 |
| `GET /api/problems/:grade/:semester/:unit` | 특정 단원의 문제지 목록 |
| `GET /problem/:grade/:semester/:unit/:filename` | 문제지 파일 제공 |

---

## ❓ 트러블슈팅

**"서버가 실행 중이 아닙니다" 오류**
→ 명령 프롬프트에서 `npm start`가 실행 중인지 확인 (창을 닫으면 서버도 꺼짐)

**"npm: 명령을 찾을 수 없습니다"**
→ Node.js 재설치 후 명령 프롬프트/PC 재시작

**새 파일 추가했는데 메뉴에 안 보임**
→ 메인 페이지 새로고침 (Ctrl+F5로 강력 새로고침)

**파일은 있는데 목록에 안 뜸**
→ 폴더 구조가 `학년\학기\단원\파일.html` 순서로 정확히 되어 있는지 확인
→ 파일 확장자가 정확히 `.html`인지 확인

**포트 3000이 이미 사용 중이라는 오류**
→ 기존에 열려있던 `npm start` 창을 닫고 다시 실행
→ 또는 `server.js`의 `PORT` 값을 3001 등으로 변경

---

## 📦 나중에 실제 웹에 배포할 때

지금은 내 컴퓨터(localhost)에서만 보이는 상태예요. 코드 안의 주소는 전부 **상대 경로**로 만들어져 있어서(`http://localhost:3000/...` 같은 절대 주소가 없음), 나중에 서버에 그대로 올려도 코드를 고칠 필요 없이 바로 작동합니다.

배포할 때 할 일:
1. `구구단닷컴` 폴더 전체(server.js, package.json, index_main.html, work 폴더)를 호스팅 서버(예: 카페24, AWS, Cafe24 등)에 업로드
2. 서버에서 `npm install && npm start` 실행
3. 대부분의 호스팅 서비스는 포트를 자동 지정해주는데, `server.js`가 이미 그 값을 자동으로 인식하도록 되어 있어서(`process.env.PORT`) 별도 설정이 필요 없어요
4. 도메인(구구단닷컴 등) 연결

이 단계는 실제로 진행할 때 다시 도와드릴게요.

---

질문이나 수정할 부분 있으면 언제든 말씀해주세요!
