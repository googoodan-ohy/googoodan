const fs=require('fs'),vm=require('vm'),assert=require('assert/strict');const c={};vm.createContext(c);for(const f of ['ko/catalog.js','ko/engine.js','de/reuse/geometry-source.js','ko/art/catalog.js','ko/art.js','de/reuse/core-numbers.js','de/reuse/unit-catalog.js','de/reuse/buildshape-units.js'])vm.runInContext(fs.readFileSync(f,'utf8'),c);const tr=s=>String(s).replaceAll('삼각형 2개의 한 변을 붙인 사각형','Ein Viereck aus zwei Dreiecken mit einer gemeinsamen Seite').replaceAll('삼각형 1개','Ein einzelnes Dreieck').replaceAll('바른 답:','Richtige Antwort:');let n=0;for(let seed=0;seed<100;seed++){const src=c.DeGeometrySource.generate('2-1-2',0,seed,1,[0,1,2].map(mode=>({skill:'flat-build',mode})));c.KoMath.generate('de-by-buildshape12',0,seed,8).forEach((s,i)=>{assert.equal(s.questions.length,1);const q=s.questions[0],o=src[i].questions[0];assert.equal(q.answer,tr(o.answer));assert.equal(q.open,o.open);assert.equal(q.visual,o.visual.replaceAll('문제 그림','Abbildung zur Aufgabe'));assert(!/[가-힣]|undefined|NaN/.test(q.prompt+q.task+q.answer+q.reason));if(i===0){assert(q.open);assert(q.task.includes('kdraw'));}n++;});}console.log('PASS '+n+' drawing activities, source answers and open-answer flag');